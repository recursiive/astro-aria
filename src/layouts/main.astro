---
import Footer from "../components/footer.astro";
import Header from "../components/header.astro";
import Dots from "../components/dots.astro";
import LoadingScreen from "../components/loading-screen.astro";
import "../assets/css/main.css";
const { 
  title = "Ryan Hunt - Cybersecurity Blog | Threat Hunting & Security Analysis", 
  description = "A cybersecurity blog focused on threat hunting, threat intelligence, and security analysis. Learn about SIEM, QRadar, incident response, and security research from a cybersecurity professional." 
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content={description} />
    <meta name="keywords" content="cybersecurity, threat hunting, threat intelligence, SIEM, QRadar, security analysis, incident response" />
    <meta name="author" content="Ryan Hunt" />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#1f2937" />
    <link rel="canonical" href="https://recursiive.ryanhunt.dev/" />
    
    <!-- Static fallback meta tags for social media -->
    <meta property="og:title" content="Ryan Hunt - Cybersecurity Blog | Threat Hunting & Security Analysis" />
    <meta property="og:description" content="A cybersecurity blog focused on threat hunting, threat intelligence, and security analysis. Learn about SIEM, QRadar, incident response, and security research." />
    <meta property="og:image" content="https://recursiive.ryanhunt.dev/assets/images/cover.png" />
    <meta name="twitter:title" content="Ryan Hunt - Cybersecurity Blog | Threat Hunting & Security Analysis" />
    <meta name="twitter:description" content="A cybersecurity blog focused on threat hunting, threat intelligence, and security analysis. Learn about SIEM, QRadar, incident response, and security research." />
    <meta name="twitter:image" content="https://recursiive.ryanhunt.dev/assets/images/cover.png" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://recursiive.ryanhunt.dev/" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://recursiive.ryanhunt.dev/assets/images/cover.png" />
    <meta property="og:image:alt" content="Ryan Hunt - Cybersecurity Blog Cover" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:site_name" content="Ryan Hunt - Cybersecurity Blog" />
    <meta property="og:locale" content="en_US" />
    
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="https://recursiive.ryanhunt.dev/assets/images/cover.png" />
    <meta name="twitter:image:alt" content="Ryan Hunt - Cybersecurity Blog Cover" />
    <meta name="twitter:creator" content="@ryanhunt" />
    <meta name="twitter:site" content="@ryanhunt" />
    
    <!-- Additional meta tags for better compatibility -->
    <meta name="application-name" content="Ryan Hunt - Cybersecurity Blog" />
    <meta name="apple-mobile-web-app-title" content="Ryan Hunt Blog" />
    <meta name="msapplication-TileColor" content="#1f2937" />
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon.png" />

    <!-- Force dark mode -->
    <script is:inline>
      document.documentElement.classList.add('dark');
    </script>

    <style>
      .prose img {
        border-radius: 20px;
      }
      
      /* Smooth page transitions */
      html {
        scroll-behavior: smooth;
      }
      
      body {
        animation: fadeIn 0.6s ease-out;
      }
      
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      /* Staggered animation for cards */
      .animate-stagger > * {
        animation: slideInUp 0.6s ease-out forwards;
        opacity: 0;
        transform: translateY(20px);
      }
      
      .animate-stagger > *:nth-child(1) { animation-delay: 0.1s; }
      .animate-stagger > *:nth-child(2) { animation-delay: 0.2s; }
      .animate-stagger > *:nth-child(3) { animation-delay: 0.3s; }
      .animate-stagger > *:nth-child(4) { animation-delay: 0.4s; }
      .animate-stagger > *:nth-child(5) { animation-delay: 0.5s; }
      .animate-stagger > *:nth-child(6) { animation-delay: 0.6s; }
      
      @keyframes slideInUp {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      /* Floating animation for subtle movement */
      @keyframes float {
        0%, 100% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-5px);
        }
      }
      
      .animate-float {
        animation: float 3s ease-in-out infinite;
      }
      
      /* Pulse animation for interactive elements */
      @keyframes pulse {
        0%, 100% {
          opacity: 1;
        }
        50% {
          opacity: 0.8;
        }
      }
      
      .animate-pulse-subtle {
        animation: pulse 2s ease-in-out infinite;
      }
      
      /* Lightbox styles */
      .lightbox {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.9);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 9999;
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease-out;
        backdrop-filter: blur(10px);
      }
      
      .lightbox.active {
        opacity: 1;
        visibility: visible;
      }
      
      .lightbox img {
        max-width: 90%;
        max-height: 90%;
        object-fit: contain;
        border-radius: 12px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        transform: scale(0.8);
        transition: transform 0.3s ease-out;
      }
      
      .lightbox.active img {
        transform: scale(1);
      }
      
      .lightbox-close {
        position: absolute;
        top: 20px;
        right: 20px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: white;
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.2s ease;
        backdrop-filter: blur(10px);
      }
      
      .lightbox-close:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: scale(1.1);
      }
      
      .lightbox-close svg {
        width: 20px;
        height: 20px;
      }
      
      /* Make images in blog posts clickable */
      .prose img {
        cursor: pointer;
        transition: transform 0.2s ease;
      }
      
      .prose img:hover {
        transform: scale(1.02);
      }
    </style>
    <link rel="icon" type="image/x-icon" href="../assets/images/favicon.png" />
    <Fragment set:html={import.meta.env.HEADER_INJECT} />
  </head>
  <body class="antialiased bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 min-h-screen">
    <LoadingScreen />
    <Dots />
    <Header />
    <slot />
    <Footer />
    
    <!-- Lightbox -->
    <div id="lightbox" class="lightbox">
      <div class="lightbox-close" onclick="closeLightbox()">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </div>
      <img id="lightbox-img" src="" alt="">
    </div>
    
    <script src="../assets/js/main.js"></script>
    <Fragment set:html={import.meta.env.FOOTER_INJECT} />
  </body>
</html>
