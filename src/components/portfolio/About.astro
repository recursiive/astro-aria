---
import { siteConfig } from "../../config";
---

<section class="relative z-20 max-w-7xl mx-auto my-12 px-7 lg:px-8">
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-start">
    <div class="lg:col-span-4 lg:pl-4">
      <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-white">
        About Me
      </h2>
      <div
        class="w-[75px] h-[5px] mt-2 rounded-full"
        style={`background-color: ${siteConfig.accentColor}`}
      >
      </div>
    </div>

    <div class="lg:col-span-8 lg:pr-8 space-y-8">
      <div class="glass-card liquid-glass rounded-xl p-6 sm:p-8 backdrop-blur-md border border-gray-700/50">
        <p class="text-lg sm:text-xl md:text-2xl leading-relaxed text-gray-300">
          {siteConfig.aboutMe}
        </p>
      </div>

      <div class="glass-card liquid-glass rounded-xl p-6 backdrop-blur-md border border-gray-700/50">
        <div class="flex flex-wrap gap-3">
          {
            siteConfig.skills.map((skill) => (
              <span class="px-3 sm:px-4 py-1.5 sm:py-2 glass-card liquid-glass border border-gray-700/50 text-gray-300 rounded-full text-sm sm:text-base md:text-lg font-medium hover:border-gray-600/70 transition-all duration-200 hover:scale-105">
                {skill}
              </span>
            ))
          }
        </div>
      </div>

    </div>
  </div>
</section>
